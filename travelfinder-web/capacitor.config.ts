import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'smart-planning-v1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
