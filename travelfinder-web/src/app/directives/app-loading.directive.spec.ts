import { AppLoadingDirective } from './app-loading.directive';

describe('AppLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new AppLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
