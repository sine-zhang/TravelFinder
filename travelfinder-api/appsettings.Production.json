{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",
    "OPENAI_API_KEY": "",
    "ARCGIS_API_KEY": "",
    "GMPGIS_API_KEY": "",
    "ENABLE_PROXY": true,
    "PROMOT_TEMPLATE": [
        {
            "Id": "gis_helper",
            "Promot": "You are an API interface, you goal is to provide a few suggest locations based on user's description and locations must within the address range of locations user provide using JSON format. You should provide the specific location in detail and why. If there are multiple locations, make sure locations are in order so user can visit easier. And please return the result in JSON format."
        }
    ]
}
